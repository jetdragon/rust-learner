# 模式匹配自检清单

使用此清单检查你对模式匹配的掌握情况。

## 基础概念

- [ ] 理解什么是模式
- [ ] 理解 match 表达式的基本语法
- [ ] 理解 match 必须穷尽所有可能
- [ ] 理解模式按顺序匹配（第一个匹配的执行）

## 模式类型

- [ ] 掌握字面值模式 (`1`, `"hello"`, `'a'`)
- [ ] 掌握变量模式 (`x`, `name`)
- [ ] 掌握通配符模式 (`_`)
- [ ] 掌握多个模式 (`1 | 2`)
- [ ] 掌握范围模式 (`1..=5`)
- [ ] 掌握解构模式

## 匹配内置类型

- [ ] 匹配整数和浮点数
- [ ] 匹配字符串和字符
- [ ] 匹配布尔值
- [ ] 匹配元组
- [ ] 匹配数组和切片
- [ ] 匹配 Option 枚举

## 解构复合类型

### 结构体解构
- [ ] 解构普通结构体 (`Point { x, y }`)
- [ ] 解构并重命名字段 (`Point { x: a, y: b }`)
- [ ] 解构并匹配字面值 (`Point { x: 0, y }`)
- [ ] 使用 `..` 忽略剩余字段

### 枚举解构
- [ ] 解构无数据变体 (`Color::Red`)
- [ ] 解构单值变体 (`Option::Some(value)`)
- [ ] 解构多值变体 (`Color::RGB(r, g, b)`)
- [ ] 解构命名变体 (`Message::Move { x, y }`)

### 元组解构
- [ ] 解构元组 (`(a, b, c)`)
- [ ] 使用 `_` 忽略元素 (`(first, _, third)`)
- [ ] 使用 `..` 忽略剩余元素

## 模式匹配高级特性

- [ ] 使用守卫（match guard）添加额外条件
- [ ] 使用 `@` 绑定同时匹配和绑定值
- [ ] 理解 `@` 绑定与变量模式的区别
- [ ] 使用 `|` 匹配多个模式之一
- [ ] 理解模式匹配的作用域和变量绑定

## if let 和 while let

- [ ] 理解 if let 的使用场景
- [ ] 使用 if let 简化单模式匹配
- [ ] 使用 if let with else
- [ ] 理解 while let 的使用场景
- [ ] 使用 while let 简化循环匹配
- [ ] 理解 if let/while let 与 full match 的区别

## 忽略值

- [ ] 使用 `_` 忽略单个值
- [ ] 使用 `..` 忽略剩余值
- [ ] 使用 `_` 在函数参数中忽略参数
- [ ] 理解 `_` 与命名变量的区别（_ 不会绑定）

## 常见模式

- [ ] 处理 Option：`Some(value)` 和 `None`
- [ ] 处理 Result：`Ok(value)` 和 `Err(error)`
- [ ] 使用 match 作为表达式返回值
- [ ] 在 match 分支中使用多个表达式（使用代码块 `{}`）
- [ ] 使用 matches! 宏进行简单模式匹配测试

## 所有权和借用

- [ ] 理解模式匹配中的所有权转移
- [ ] 在模式中使用引用避免转移
- [ ] 理解 match 如何处理可变引用
- [ ] 理解模式匹配中的 Copy 类型行为

## 最佳实践

- [ ] 将更具体的模式放在更通用的模式之前
- [ ] 使用 `_` 处理不需要的情况
- [ ] 使用 if let 简化只关心一种情况
- [ ] 使用 while let 迭代匹配过程
- [ ] 使用守卫而不是嵌套 match
- [ ] 为复杂模式添加注释

## 实际应用

- [ ] 在实际代码中使用 match 处理枚举
- [ ] 使用模式匹配解析和验证输入
- [ ] 使用解构简化代码
- [ ] 使用模式匹配实现状态机
- [ ] 使用 if let 处理可能失败的操作
- [ ] 使用 matches! 宏编写测试

## 练习完成情况

- [ ] 完成所有基础练习题
- [ ] 完成中等难度练习题
- [ ] 完成高级练习题
- [ ] 完成综合练习项目
- [ ] 通过所有测试用例

## 自我测试

尝试回答以下问题：

1. match 表达式为什么必须穷尽所有可能？
2. `_` 通配符和变量模式有什么区别？
3. 什么时候使用 if let 而不是 match？
4. `@` 绑定的作用是什么？
5. 如何在模式中添加额外的条件判断？
6. `..` 在解构中如何工作？
7. 如何匹配多个模式之一？
8. 模式匹配的顺序重要吗？为什么？

## 代码能力检查

不看资料，能否完成以下任务：

- [ ] 写一个 match 表达式处理自定义枚举
- [ ] 使用解构从结构体中提取特定字段
- [ ] 使用守卫添加复杂的匹配条件
- [ ] 使用 @ 绑定同时测试和绑定值
- [ ] 使用 if let 简化 Option 处理
- [ ] 使用 while let 处理迭代过程
- [ ] 正确处理模式匹配中的所有权问题

## 下一步学习

完成本模块后，继续学习：

- [ ] [module-05-error-handling](../module-05-error-handling/) - 错误处理
- [ ] [module-06-collections](../module-06-collections/) - 集合类型
- [ ] [module-07-traits](../module-07-traits/) - 泛型和 Trait

## 掌握程度自评

- **初学者**: 需要频繁查阅资料，能完成基本练习
- **中级**: 偶尔查阅资料，能独立解决常见问题
- **熟练**: 很少查阅资料，能写出符合 Rust 习惯的代码
- **专家**: 能指导他人，理解设计权衡和最佳实践

你的掌握程度: ___________
