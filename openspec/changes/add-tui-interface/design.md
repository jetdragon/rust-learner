# 设计文档：交互式终端界面 (TUI)

## 架构概览

```
┌─────────────────────────────────────────────────────────┐
│                      main.rs                            │
│  ┌─────────────┐    ┌─────────────┐                    │
│  │   CLI 模式   │    │   TUI 模式   │                    │
│  │  (现有)     │    │   (新增)     │                    │
│  └─────────────┘    └──────┬──────┘                    │
│                            │                             │
│  ┌─────────────────────────┼─────────────────────────┐ │
│  │                         │                         │ │
│  ▼                         ▼                         │ │
│ ┌─────────┐          ┌─────────────┐                  │ │
│ │  ui.rs  │          │   tui.rs    │                  │ │
│ │(文本输出)│          │ (交互界面)  │                  │ │
│ └─────────┘          └──────┬──────┘                  │ │
│                              │                          │ │
│         ┌────────────────────┼────────────────────┐    │ │
│         │                    │                    │    │ │
│         ▼                    ▼                    ▼    │ │
│    ┌─────────┐         ┌─────────┐         ┌─────────┐│ │
│    │ db.rs   │         │exercise │         │progress ││ │
│    │(数据层)  │         │  .rs    │         │  .rs    ││ │
│    └─────────┘         └─────────┘         └─────────┘│ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 组件设计

### 1. 应用状态管理 (AppState)

```rust
pub enum AppState {
    MainMenu,
    Dashboard,
    UpdateProgress { selected_module: usize, selected_task: usize },
    Practice { selected_module: usize, question_count: usize },
    Achievements,
    RemindSetup { hour: u8, minute: u8 },
    Export,
}
```

### 2. TUI 主循环

```rust
pub fn run_tui() -> Result<()> {
    let mut terminal = init_terminal()?;
    let mut app = App::new();

    loop {
        terminal.draw(|f| ui::draw_app(f, &mut app))?;

        if let Event::Key(key) = event::read()? {
            match key.code {
                KeyCode::Char('q') => break,
                KeyCode::Esc => app.go_back(),
                KeyCode::Up => app.select_prev(),
                KeyCode::Down => app.select_next(),
                KeyCode::Enter => app.confirm(),
                _ => {}
            }
        }
    }

    restore_terminal()
}
```

### 3. 界面组件层次

```
App
├── MainMenu (主菜单)
│   └── List<String> (选项列表)
│
├── Dashboard (仪表板)
│   ├── ProgressChart (进度图表)
│   ├── ModuleList (模块列表)
│   └── StatsPanel (统计面板)
│
├── UpdateProgress (更新进度)
│   ├── ModuleSelector (模块选择器)
│   └── TaskCheckboxGroup (任务复选框组)
│
├── Practice (练习)
│   ├── ModuleSelector (模块选择)
│   ├── QuestionCounter (题目计数)
│   └── QuestionView (题目视图)
│
└── RemindSetup (提醒设置)
    ├── TimeInput (时间输入)
    └── ConfirmButton (确认按钮)
```

## 屏幕布局设计

### 主菜单

```
┌──────────────────────────────────────────────────────┐
│                      🦀 Rust 学习伴侣                  │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ╔═════════════════════════════════════════════════╗ │
│  ║                                                  ║ │
│  ║    📊  查看学习仪表板                            ║ │
│  ║                                                  ║ │
│  ║    📚  更新学习进度                              ║ │
│  ║                                                  ║ │
│  ║    ✏️   开始练习测试                              ║ │
│  ║                                                  ║ │
│  ║    🏆  查看成就                                  ║ │
│  ║                                                  ║ │
│  ║    ⏰  设置学习提醒                              ║ │
│  ║                                                  ║ │
│  ║    📤  导出学习数据                              ║ │
│  ║                                                  ║ │
│  ╚═════════════════════════════════════════════════╝ │
│                                                       │
│                                                       │
│  提示: ↑↓ 移动光标 | Enter 确认 | q 退出               │
└──────────────────────────────────────────────────────┘
```

### 更新进度界面

```
┌──────────────────────────────────────────────────────┐
│  📚 更新学习进度                      [Esc] 返回       │
├──────────────────────────────────────────────────────┤
│                                                       │
│  选择模块:                                            │
│  ┌─────────────────────────────────────────────────┐ │
│  │ > 01-基础入门 (module-01-basics)                │ │
│  │   02-所有权与借用                                │ │
│  │   03-结构体与枚举                                │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  标记完成的任务:                                      │
│  ┌─────────────────────────────────────────────────┐ │
│  │ [✓] 概念学习                                    │ │
│  │ [ ] 代码示例                                    │ │
│  │ [ ] 练习题                                      │ │
│  │ [ ] 综合练习                                    │ │
│  │ [ ] 自检通过                                    │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌────────────┐  ┌────────────┐                     │
│  │    取消     │  │    保存     │                     │
│  └────────────┘  └────────────┘                     │
│                                                       │
│  提示: ↑↓ 选择模块 | Tab 切换 | 空格 选中              │
└──────────────────────────────────────────────────────┘
```

### 练习界面

```
┌──────────────────────────────────────────────────────┐
│  ✏️  练习测试 - 01-基础入门              [Esc] 退出    │
├──────────────────────────────────────────────────────┤
│                                                       │
│  题目 3/5                          得分: 66.7%        │
│  ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │                                                 │ │
│  │  [数据类型]                                      │ │
│  │                                                 │ │
│  │  i32 类型的整数范围是多少？                      │ │
│  │                                                 │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  选项:                                                │
│  ┌─────────────────────────────────────────────────┐ │
│  │   1. -128 到 127                                │ │
│  │   2. 0 到 255                                    │ │
│  │ > 3. -2^31 到 2^31-1                            │ │
│  │   4. -2^63 到 2^63-1                            │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐     │
│  │    上一题   │  │    确认     │  │    下一题   │     │
│  └────────────┘  └────────────┘  └────────────┘     │
│                                                       │
└──────────────────────────────────────────────────────┘
```

## 事件处理流程

```
用户输入
    │
    ▼
crossterm::event()
    │
    ▼
事件映射 (KeyCode -> Action)
    │
    ▼
状态更新 (App::handle_action)
    │
    ▼
界面重绘 (App::render)
```

## 数据流

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   用户输入    │ ──> │  事件处理     │ ──> │  状态变更     │
│  (键盘事件)   │     │  (Action)    │     │ (AppState)   │
└──────────────┘     └──────────────┘     └──────────────┘
                                                   │
                                                   ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  业务逻辑     │ <── │  数据查询     │ <── │  UI 渲染      │
│ (exercise,   │     │  (db, repo)  │     │  (ratatui)   │
│  progress)   │     │              │     │              │
└──────────────┘     └──────────────┘     └──────────────┘
```

## 技术约束

1. **最小终端尺寸**: 80x24 字符
2. **颜色支持**: 使用 16 色 ANSI，确保广泛兼容
3. **刷新率**: 仅在事件触发时重绘
4. **错误处理**: TUI 内部错误用弹窗显示，不崩溃

## 迁移策略

### 阶段 1: 基础框架
- 创建 tui.rs 基础结构
- 实现主菜单界面
- 基本事件循环

### 阶段 2: 功能集成
- 实现仪表板视图
- 实现进度更新界面
- 实现练习界面

### 阶段 3: 完善
- 成就视图
- 提醒设置
- 数据导出
- 帮助文档
